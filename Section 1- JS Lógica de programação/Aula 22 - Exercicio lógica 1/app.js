function numeros(num1, num2) {
  //   return num1 > num2
  //     ? `O maior número é o primeiro ${num1}`
  //     : `O maior número é o segundo ${num2}`;
  //   if (num1 > num2) return console.log(`O maior número é o primeiro ${num1}`);
  //   return console.log(`O maior número é o segundo ${num2}`);
}

const numeros2 = (x, y) => (x > y ? x : y);
console.log(numeros2(27, 52));
